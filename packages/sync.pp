package { 'googledrive':
  ensure => latest
}

package { 'dropbox':
  ensure => latest
}

package { 'seafile':
  ensure => latest
}

package { 'owncloudclient':
  ensure => latest
}

package { 'megasync':
  ensure => latest
}
